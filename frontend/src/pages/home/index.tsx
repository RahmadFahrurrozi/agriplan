import { Button } from "@/components/ui/button";

const HomePage = () => {
  return (
    <div className="flex justify-center items-center flex-col gap-2 align-middle">
      <div>Home Page</div>
      <Button variant={"default"}>Isi Ja sendiri</Button>
    </div>
  );
};

export default HomePage;
